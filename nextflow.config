process.container = 'quay.io/nextflow/bash'

profiles {
    awsbatch {
        aws.region = 'us-east-2'
        aws.batch.cliPath = '/home/ec2-user/miniconda/bin/aws'
        process.executor = 'awsbatch'
        process.queue = 'Seqera-work-spot'
        docker.enabled = true
    }
}
